import styled from "styled-components";

export const Regulador = styled.div`
    display: flex;

    *{
        border-color: var(--text) !important;
        background-color: var(--background) !important;
        color: var(--text) !important;
        text-transform: capitalize;
    };

`
